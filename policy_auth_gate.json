{
  "policy_id": "auth_gate_v1",
  "rules": [
    {
      "rule": "no_direct_commit_from_compute_nodes",
      "description": "Compute/Orchestrator nodes (e.g. DAIOF) cannot commit directly to Canon",
      "enforcement": "must pass through validation layer"
    },
    {
      "rule": "event_must_be_signed",
      "description": "All events must include origin + signature placeholder",
      "enforcement": "reject_if_missing"
    },
    {
      "rule": "canon_append_only",
      "description": "No overwrite or deletion allowed",
      "enforcement": "append_only"
    },
    {
      "rule": "identity_primary_signals_override",
      "description": "Personal email addresses, phone numbers, and Telegram nicks are canonical identifiers for local sovereignty and take precedence in auth/identity resolution.",
      "enforcement": "treat_as_primary"
    }
  ],
  "identity_overrides": {
    "canonical_categories": ["personal_email", "phone_number", "telegram_nick"],
    "scope": "local_sovereignty",
    "primary_auth_signals": true,
    "notes": "These categories override secondary identity filters in auth resolution."
  },
  "timestamp": "2026-04-20"
}